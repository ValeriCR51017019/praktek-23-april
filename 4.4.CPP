#include<iostream.h>
#include<conio.h>

main()
{
	int bil;

   cout<<"masukkan bilangan yang akan dicek = ";
   cin>>bil;

   if (nilai > 0)
   	cout<<"adalah bilangan positif";
   else if (bil < 0)
     	cout<<"adalah bilangan negatif";
   else
   	cout<<"anda menginput bilangan no1 (0)";
   getch();
}
